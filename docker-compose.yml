services:
  app:
    build: .
    volumes:
      - .:/usr/src/app
      - ~/.aws:/root/.aws:ro
    working_dir: /usr/src/app
    command: pipenv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
      - "50051:50051"
    env_file:
      - .env